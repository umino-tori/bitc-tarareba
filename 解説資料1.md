# BTCタラレバ長者 vs 現実の地獄シミュレーター 解説資料

---

## 1. シミュレーション情報源の解説

### ロウソク足（始値・最安値・最高値・終値）

| 項目 | 説明 | コード上の変数 |
|------|------|---------------|
| 始値（Open） | その日の取引開始時の価格 | `df["Open"]` |
| 最安値（Low） | その日の最も安い価格（A/Bシナリオの「買値」として使用） | `lows[i]` |
| 最高値（High） | その日の最も高い価格（A/Bシナリオの「売値」として使用） | `highs[i]` |
| 終値（Close） | その日の取引終了時の価格（Cシナリオで資産評価に使用） | `closes[i]` |

**データ取得元:** yfinance から BTC-JPY の日足OHLC データを取得し、SQLite DB (`btc_data.db`) に保存して使用します。

---

### 初期資金（円）

- サイドバーの数値入力で設定（デフォルト: **100万円**、範囲: 1万円〜10億円）
- コード変数: `initial_fund`
- A・Bシナリオでは「毎日この金額で売買する元手」として使用
- Cシナリオでは「証拠金（担保）」として使用

---

### 手数料（Maker/Taker）の %

- サイドバーのスライダーで設定（デフォルト: **0.1%**、範囲: 0〜5%）
- コード変数: `fee_rate` → 内部計算では `fee_pct = fee_rate / 100.0`
- **適用方法:** 買いと売り、それぞれ1回ずつ（合計2回）かかる
  - 買い手数料 = 投資額 × fee_pct
  - 売り手数料 = 売却額 × fee_pct
- **Aシナリオ（Dream）では手数料ゼロ**（夢の理論値）
- **Bシナリオ（Reality）で手数料が発生し、毎日じわじわ資産を削る**

---

### スリッページ %

- サイドバーのスライダーで設定（デフォルト: **0.5%**、範囲: 0〜5%）
- コード変数: `slippage_rate` → 内部計算では `slip_pct = slippage_rate / 100.0`
- **スリッページとは:** 注文を出したときの想定価格と、実際に約定した価格のズレ（市場の流動性不足・大口注文の影響等）
- 手数料と同様に、買いと売りで各1回ずつ発生
- **Aシナリオではゼロ、Bシナリオで発生**

---

### レバレッジ倍率

- サイドバーのスライダーで設定（デフォルト: **1倍**、範囲: 1〜100倍）
- コード変数: `leverage`
- **Cシナリオ（Hell）のみ使用**
- 価格変動の損益がレバレッジ倍率分だけ増幅される
  - 例: 10倍レバレッジで価格が5%上昇 → 資産は50%増加
  - 逆に価格が5%下落 → 資産は50%減少（大きなリスク）

---

### 強制ロスカットライン（証拠金維持率）%

- サイドバーのスライダーで設定（デフォルト: **50%**、範囲: 10〜100%）
- コード変数: `lc_maintenance` → 内部計算では `lc_threshold = lc_maintenance / 100.0`
- **証拠金維持率とは:** 現在の資産評価額 ÷ 初期投資額（証拠金）× 100
- この維持率がロスカットラインを下回った瞬間、強制的に全資産が清算される
- **Cシナリオ（Hell）のみ適用**

---

## 2. 計算方法

### 初期資金の含み益の計算

#### A（Dream）の資産計算

```
日次損益 = 初期資金 × (最高値 / 最安値 − 1)
累積損益 += 日次損益
本日の資産額 = 初期資金 + 累積損益
```

> **ポイント:** 毎日「初期資金」を元手として売買しているため、厳密な複利ではありません。
> 利益は累積加算されますが、売買の元手は常に `initial_fund` 固定です。

#### B（Reality）の資産計算

```
コスト率 = (手数料% + スリッページ%) × 2  ← 買い+売りで各2回
日次損益 = 初期資金 × (最高値 / 最安値 × (1 − コスト率) − 1)
累積損益 += 日次損益
本日の資産額 = 初期資金 + 累積損益
```

> `(1 − コスト率)` を最高値/最安値の比率に掛けることで、売却額から手数料・スリッページを差し引いています。

#### C（Hell）の資産計算

```
価格変動率 = (当日終値 − エントリー終値) / エントリー終値
損益率 = 価格変動率 × レバレッジ倍率
本日の資産額 = 初期資金 × (1 + 損益率)
```

> エントリー価格は**初日の終値（closes[0]）**に固定されます（Buy & Hold）。

---

### 日々の損益計算

各日の損益は「前日の資産額」と「当日の資産額」の差分で計算されます。

```
A: 日次損益[i] = dream_assets[i+1]   − dream_assets[i]
B: 日次損益[i] = reality_assets[i+1] − reality_assets[i]
C: 日次損益[i] = hell_assets[i+1]    − hell_assets[i]
```

これがグラフ下段の「日次売買損益」エリアに表示される値です。

**詳細テーブルの手数料内訳（参考値）:**

| 項目 | 計算式 |
|------|--------|
| 買い手数料 | 初期資金 × 手数料% |
| 売り手数料 | (初期資金 × 最高値/最安値) × 手数料% |
| スリッページ計 | 初期資金 × スリッページ% × 2（買い+売り） |
| 売買差額（グロス） | 初期資金 × (最高値/最安値 − 1) |

---

### 強制ロスカットライン計算と判定方法

```
証拠金維持率 = 現在の資産評価額 / 初期資金（証拠金）

判定:
  証拠金維持率 ≤ ロスカットライン(%) / 100
    → ロスカット発動 → 資産を 0 円にして以降も 0 固定
```

**例（初期資金100万円、レバレッジ10倍、ロスカットライン50%の場合）:**

| BTC価格変動 | 損益率 | 資産評価額 | 証拠金維持率 | 判定 |
|------------|--------|-----------|------------|------|
| +5%        | +50%   | 150万円    | 150%       | 正常 |
| 0%         | 0%     | 100万円    | 100%       | 正常 |
| −3%        | −30%   | 70万円     | 70%        | 正常 |
| −5%        | −50%   | 50万円     | 50%        | ロスカット発動 |
| −10%       | −100%  | 0円        | 0%         | 既にロスカット済 |

ロスカット発動後は、`liquidated = True` フラグが立ち、以降の日は資産=0円として記録されます。

---

## 3. シミュレーション種別ごとの計算解説

### A: 神の現物（Dream）

**コンセプト:** 毎日「その日の最安値で買い・最高値で売る」という神業トレードの理論最大値

**ルール:**
- 毎日、最安値（Low）で `initial_fund` 円分のBTCを購入
- 同日の最高値（High）で全て売却
- 手数料・スリッページ: **ゼロ（夢の世界）**
- 損益は毎日累積

**計算式:**
```
日次損益 = 初期資金 × (最高値 / 最安値 − 1)
```

**特徴:** BTCの日中値幅が大きければ大きいほど、桁外れの利益が積み重なる。現実には不可能な理論上の最大値として機能する。

---

### B: 現実のトレード（Reality）

**コンセプト:** Aと同じトレードを行うが、毎回手数料とスリッページが発生する「現実」

**ルール:**
- トレードロジックはAと全く同じ（最安値買い・最高値売り）
- 買いのたびに: 手数料% + スリッページ% のコスト発生
- 売りのたびに: 手数料% + スリッページ% のコスト発生
- 合計コスト率 = `(手数料% + スリッページ%) × 2`

**計算式:**
```
コスト率 = (fee_pct + slip_pct) × 2
日次損益 = 初期資金 × (最高値 / 最安値 × (1 − コスト率) − 1)
```

**Aとの差額 = 「手数料・スリッページによる損失総額」** として画面下部に赤字で表示されます。

**特徴:** デフォルト設定（手数料0.1%+スリッページ0.5%）でも毎日の売買を繰り返すと、長期では甚大な損失となることを可視化します（手数料負け）。

---

### C: レバレッジ・ガチホ（Hell）

**コンセプト:** 初日に全額でロングポジションを持ち、レバレッジを効かせたまま保持し続ける（退場リスクあり）

**ルール:**
- エントリー: **初日の終値（Close）** でロングポジション
- 以降は価格変動をレバレッジ倍率で増幅した損益を毎日計算
- ロスカットラインを一度でも下回ったら**即退場（資産=0、以降ずっと0）**
- 手数料・スリッページは発生しない（ガチホなので売買しない）

**計算式:**
```
価格変動率 = (当日終値 − 初日終値) / 初日終値
損益率 = 価格変動率 × レバレッジ倍率
現在資産 = 初期資金 × (1 + 損益率)
証拠金維持率 = 現在資産 / 初期資金

証拠金維持率 ≤ ロスカットライン → 全資産消滅
```

**特徴:** レバレッジが高いほど利益も損失も拡大。BTCの価格が一時的に大きく下落するだけでロスカットが発動し、以後の価格回復も関係なく退場となる。

---

### 補足: C でレバレッジ倍率1倍にすると「通常のガチホ」になりますか？

**ほぼ同等になりますが、完全に同一ではありません。**

#### 同一点
レバレッジ1倍のとき:
```
損益率 = 価格変動率 × 1 = (当日終値 − 初日終値) / 初日終値
現在資産 = 初期資金 × (1 + 価格変動率)
```
これは「初日に全額BTC購入して保有し続ける」という通常のガチホと**同じ計算式**です。

#### 相違点
通常のガチホでは「価格がどれだけ下がっても持ち続けられる（ロスカットなし）」ですが、このシミュレーションでは**レバレッジ1倍でも強制ロスカットが判定**されます。

| ロスカットライン | ロスカット発動の条件（レバレッジ1倍時） |
|----------------|--------------------------------------|
| 50%（デフォルト） | BTCが初日比 **−50%以上** 下落したとき |
| 10%（最小値） | BTCが初日比 **−90%以上** 下落したとき |

> **まとめ:** レバレッジ1倍・ロスカットライン10〜20%程度の設定であれば、実質的に「通常のガチホ」に近い挙動になります。ロスカットラインを低く設定するほど、ガチホに近づきます。

---

*※ このシミュレーションは過去のBTC-JPY価格データに基づく理論値です。実際の取引結果を保証するものではありません。*
